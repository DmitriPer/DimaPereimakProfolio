{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build --workspace=apps/backend && cd apps/backend && npx prisma migrate deploy"
  },
  "deploy": {
    "startCommand": "cd apps/backend && node dist/main"
  }
}
